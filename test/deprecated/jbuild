(library
 ((name test_ppx_sexp_value_deprecated_lib)
  (libraries (core sexplib))
  (extra_disabled_warnings (22))
  (preprocess (((pps (ppx_sexp_conv ppx_here ppx_assert ppx_sexp_value ppx_inline_test)) All)))))
