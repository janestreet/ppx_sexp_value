
(library
 ((name example_ppx_sexp_value_deprecated_lib)
  (libraries (core))
  (extra_disabled_warnings (22))
  (preprocess (((pps (ppx_sexp_value)) All)))))

(alias ((name DEFAULT) (deps (example.ml.pp))))
